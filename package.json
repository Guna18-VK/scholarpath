{
  "name": "scholarpath-root",
  "version": "1.0.0",
  "description": "Scholarship Awareness and Recommendation System",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd backend && npm install",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
